{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://serverlessinbox.com/schemas/jmap/methods/AddressBook.changes.args.schema.json",
  "title": "AddressBook/changes args",
  "type": "object",
  "properties": {
    "accountId": { "$ref": "../core/types.schema.json#/$defs/AccountId", "description": "The account to fetch address book changes for." },
    "sinceState": { "type": "string", "minLength": 1, "description": "The state string from the last AddressBook/get or AddressBook/changes response." },
    "maxChanges": { "type": "integer", "minimum": 0, "description": "Maximum number of change records to return. If exceeded, hasMoreChanges will be true." }
  },
  "required": ["accountId", "sinceState"],
  "additionalProperties": false
}
