{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://serverlessinbox.com/schemas/jmap/methods/Thread.get.args.schema.json",
  "title": "Thread/get args",
  "type": "object",
  "properties": {
    "accountId": { "$ref": "../core/types.schema.json#/$defs/AccountId", "description": "The account to fetch threads from." },
    "ids": {
      "type": "array",
      "description": "IDs of specific threads to fetch.",
      "items": { "$ref": "../core/types.schema.json#/$defs/ThreadId" }
    },
    "properties": {
      "type": "array",
      "description": "Thread properties to include. Omit for all properties.",
      "items": { "type": "string", "minLength": 1 }
    }
  },
  "required": ["accountId"],
  "additionalProperties": false
}
