{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://serverlessinbox.com/schemas/jmap/methods/Mailbox.get.response.schema.json",
  "title": "Mailbox/get response",
  "type": "object",
  "properties": {
    "accountId": { "$ref": "../core/types.schema.json#/$defs/AccountId", "description": "The account the mailboxes belong to." },
    "state": { "type": "string", "description": "The current state of the Mailbox type. Pass to Mailbox/changes to detect future changes." },
    "list": {
      "type": "array",
      "description": "The list of Mailbox objects that were found.",
      "items": { "$ref": "../core/types.schema.json#/$defs/MailboxExt" }
    },
    "notFound": {
      "type": "array",
      "description": "IDs from the request that could not be found.",
      "items": { "type": "string" }
    }
  },
  "required": ["accountId", "state", "list"],
  "additionalProperties": false
}
