{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://serverlessinbox.com/schemas/jmap/methods/ContactCard.changes.args.schema.json",
  "title": "ContactCard/changes args",
  "type": "object",
  "properties": {
    "accountId": { "$ref": "../core/types.schema.json#/$defs/AccountId", "description": "The account to fetch contact card changes for." },
    "sinceState": { "type": "string", "minLength": 1, "description": "The state string from the last ContactCard/get or ContactCard/changes response." },
    "maxChanges": { "type": "integer", "minimum": 0, "description": "Maximum number of change records to return. If exceeded, hasMoreChanges will be true." }
  },
  "required": ["accountId", "sinceState"],
  "additionalProperties": false
}
