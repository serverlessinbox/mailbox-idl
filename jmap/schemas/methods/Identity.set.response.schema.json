{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://serverlessinbox.com/schemas/jmap/methods/Identity.set.response.schema.json",
  "title": "Identity/set response",
  "description": "Response for Identity/set (RFC 8621 §6.3 / RFC 8620 §5.3).",
  "type": "object",
  "properties": {
    "accountId": { "$ref": "../core/types.schema.json#/$defs/AccountId", "description": "The account the set operation was performed on." },
    "oldState": { "type": ["string", "null"], "description": "The Identity state before this call, or null if no prior state existed." },
    "newState": { "type": "string", "description": "The Identity state after this call." },
    "created": {
      "type": ["object", "null"],
      "description": "Map of creation id to the created Identity object (server-initiated only).",
      "propertyNames": { "$ref": "../core/types.schema.json#/$defs/CreationId" },
      "additionalProperties": { "$ref": "../core/types.schema.json#/$defs/Identity" }
    },
    "updated": {
      "type": ["object", "null"],
      "description": "Map of id to the updated Identity object (or null if unchanged properties were not returned).",
      "propertyNames": { "$ref": "../core/types.schema.json#/$defs/Id" },
      "additionalProperties": {
        "oneOf": [
          { "$ref": "../core/types.schema.json#/$defs/Identity" },
          { "type": "null" }
        ]
      }
    },
    "destroyed": {
      "type": ["array", "null"],
      "description": "Ids of Identity objects that were successfully destroyed (server-initiated only).",
      "items": { "$ref": "../core/types.schema.json#/$defs/Id" }
    },
    "notCreated": {
      "type": ["object", "null"],
      "description": "Map of creation id to SetError for each failed create.",
      "propertyNames": { "$ref": "../core/types.schema.json#/$defs/CreationId" },
      "additionalProperties": { "$ref": "../core/types.schema.json#/$defs/SetError" }
    },
    "notUpdated": {
      "type": ["object", "null"],
      "description": "Map of id to SetError for each failed update.",
      "propertyNames": { "$ref": "../core/types.schema.json#/$defs/Id" },
      "additionalProperties": { "$ref": "../core/types.schema.json#/$defs/SetError" }
    },
    "notDestroyed": {
      "type": ["object", "null"],
      "description": "Map of id to SetError for each failed destroy.",
      "propertyNames": { "$ref": "../core/types.schema.json#/$defs/Id" },
      "additionalProperties": { "$ref": "../core/types.schema.json#/$defs/SetError" }
    }
  },
  "required": ["accountId", "oldState", "newState"],
  "additionalProperties": false
}
