{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://serverlessinbox.com/schemas/jmap/methods/AddressBook.get.response.schema.json",
  "title": "AddressBook/get response",
  "type": "object",
  "properties": {
    "accountId": { "$ref": "../core/types.schema.json#/$defs/AccountId", "description": "The account the address books belong to." },
    "state": { "type": "string", "description": "The current state of the AddressBook type. Pass to AddressBook/changes to detect future changes." },
    "list": {
      "type": "array",
      "description": "The list of AddressBook objects that were found.",
      "items": { "$ref": "../core/types.schema.json#/$defs/AddressBook" }
    },
    "notFound": {
      "type": "array",
      "description": "IDs from the request that could not be found.",
      "items": { "type": "string" }
    }
  },
  "required": ["accountId", "state", "list", "notFound"],
  "additionalProperties": false
}
