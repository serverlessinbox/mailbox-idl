{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://serverlessinbox.com/schemas/jmap/methods/ShareNotification.changes.args.schema.json",
  "title": "ShareNotification/changes args",
  "type": "object",
  "properties": {
    "accountId": { "$ref": "../core/types.schema.json#/$defs/AccountId", "description": "The account to fetch share notification changes for." },
    "sinceState": { "type": "string", "minLength": 1, "description": "The state string from the last ShareNotification/get or ShareNotification/changes response." },
    "maxChanges": { "type": "integer", "minimum": 0, "description": "Maximum number of change records to return. If exceeded, hasMoreChanges will be true." }
  },
  "required": ["accountId", "sinceState"],
  "additionalProperties": false
}
