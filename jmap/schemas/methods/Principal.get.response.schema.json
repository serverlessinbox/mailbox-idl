{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://serverlessinbox.com/schemas/jmap/methods/Principal.get.response.schema.json",
  "title": "Principal/get response",
  "type": "object",
  "properties": {
    "accountId": { "$ref": "../core/types.schema.json#/$defs/AccountId", "description": "The account the principals belong to." },
    "state": { "type": "string", "description": "The current state of the Principal type. Pass to Principal/changes to detect future changes." },
    "list": {
      "type": "array",
      "description": "The list of Principal objects that were found.",
      "items": { "$ref": "../core/types.schema.json#/$defs/Principal" }
    },
    "notFound": {
      "type": "array",
      "description": "IDs from the request that could not be found.",
      "items": { "type": "string" }
    }
  },
  "required": ["accountId", "state", "list", "notFound"],
  "additionalProperties": false
}
