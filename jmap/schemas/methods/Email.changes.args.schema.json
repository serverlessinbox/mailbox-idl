{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://serverlessinbox.com/schemas/jmap/methods/Email.changes.args.schema.json",
  "title": "Email/changes args",
  "type": "object",
  "properties": {
    "accountId": { "$ref": "../core/types.schema.json#/$defs/AccountId", "description": "The account to fetch changes for." },
    "sinceState": { "type": "string", "minLength": 1, "description": "The state string from the last Email/get or Email/changes response. Only changes after this state are returned." },
    "maxChanges": { "type": "integer", "minimum": 0, "description": "Maximum number of change records to return. Omit for no limit. If exceeded, hasMoreChanges will be true." }
  },
  "required": ["accountId", "sinceState"],
  "additionalProperties": false
}
