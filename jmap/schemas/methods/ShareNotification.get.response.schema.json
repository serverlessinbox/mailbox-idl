{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://serverlessinbox.com/schemas/jmap/methods/ShareNotification.get.response.schema.json",
  "title": "ShareNotification/get response",
  "type": "object",
  "properties": {
    "accountId": { "$ref": "../core/types.schema.json#/$defs/AccountId", "description": "The account the share notifications belong to." },
    "state": { "type": "string", "description": "The current state of the ShareNotification type. Pass to ShareNotification/changes to detect future changes." },
    "list": {
      "type": "array",
      "description": "The list of ShareNotification objects that were found.",
      "items": { "$ref": "../core/types.schema.json#/$defs/ShareNotification" }
    },
    "notFound": {
      "type": "array",
      "description": "IDs from the request that could not be found.",
      "items": { "type": "string" }
    }
  },
  "required": ["accountId", "state", "list", "notFound"],
  "additionalProperties": false
}
