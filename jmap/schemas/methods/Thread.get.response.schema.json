{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://serverlessinbox.com/schemas/jmap/methods/Thread.get.response.schema.json",
  "title": "Thread/get response",
  "type": "object",
  "properties": {
    "accountId": { "$ref": "../core/types.schema.json#/$defs/AccountId", "description": "The account the threads belong to." },
    "state": { "type": "string", "minLength": 1, "description": "The current state of the Thread type." },
    "list": {
      "description": "The list of Thread objects that were found.",
      "type": "array",
      "items": {
        "allOf": [
          {
            "type": "object",
            "properties": {
              "id": { "$ref": "../core/types.schema.json#/$defs/ThreadId" },
              "emailIds": {
                "type": "array",
                "items": { "$ref": "../core/types.schema.json#/$defs/EmailId" }
              }
            },
            "required": ["id", "emailIds"],
            "additionalProperties": false
          },
          {
            "type": "object",
            "patternProperties": {
              "^sib:": {}
            }
          }
        ],
        "unevaluatedProperties": false
      }
    },
    "notFound": {
      "type": "array",
      "description": "IDs from the request that could not be found.",
      "items": { "$ref": "../core/types.schema.json#/$defs/ThreadId" }
    }
  },
  "required": ["accountId", "state", "list"],
  "additionalProperties": false
}
