// Code generated by protoc-gen-ts-client. DO NOT EDIT.

import type { Message } from '@bufbuild/protobuf';
import { create, toJson, fromJson } from '@bufbuild/protobuf';

import type {
  ListUsersRequest,
  ListUsersResponse,
  GetUserRequest,
  GetUserResponse,
  UpdateUserRequest,
  UpdateUserResponse,
  DeleteUserRequest,
  DeleteUserResponse,
  AddUserAccountCapabilityRequest,
  AddUserAccountCapabilityResponse,
} from './users_pb';

import {
  ListUsersRequestSchema,
  ListUsersResponseSchema,
  GetUserRequestSchema,
  GetUserResponseSchema,
  UpdateUserRequestSchema,
  UpdateUserResponseSchema,
  DeleteUserRequestSchema,
  DeleteUserResponseSchema,
  AddUserAccountCapabilityRequestSchema,
  AddUserAccountCapabilityResponseSchema,
} from './users_pb';

// Request types without protobuf Message metadata
type ListUsersRequestData = Omit<ListUsersRequest, keyof Message>;
type GetUserRequestData = Omit<GetUserRequest, keyof Message>;
type UpdateUserRequestData = Omit<UpdateUserRequest, keyof Message>;
type DeleteUserRequestData = Omit<DeleteUserRequest, keyof Message>;
type AddUserAccountCapabilityRequestData = Omit<AddUserAccountCapabilityRequest, keyof Message>;

export interface UsersServiceClientConfig {
  endpoint: string;
  authorization?: string;
  /** Custom fetch function for request interceptors, token refresh, retry logic, etc. */
  fetch?: typeof fetch;
}

/**
 * UsersService provides operations for managing users and their primary accounts.
 */
export class UsersServiceClient {
  private readonly endpoint: string;
  private readonly authorization?: string;
  private readonly fetch: typeof fetch;

  constructor(config: UsersServiceClientConfig) {
    this.endpoint = config.endpoint.replace(/\/$/, '');
    this.authorization = config.authorization;
    this.fetch = config.fetch || globalThis.fetch.bind(globalThis);
  }

  /**
   * ListUsers retrieves all users with pagination.
   */
  async listUsers(request?: ListUsersRequestData): Promise<ListUsersResponse> {
    const headers: Record<string, string> = {
      'Content-Type': 'application/json',
    };

    if (this.authorization) {
      headers['Authorization'] = this.authorization;
    }

    const requestMessage = create(ListUsersRequestSchema, request || {});
    const response = await this.fetch(`${this.endpoint}/ListUsers`, {
      method: 'POST',
      headers,
      body: JSON.stringify(toJson(ListUsersRequestSchema, requestMessage)),
    });

    if (!response.ok) {
      const error = await response.text();
      throw new Error(`ListUsers failed: ${response.status} ${error}`);
    }

    const responseData = (await response.json()) as any;
    return fromJson(ListUsersResponseSchema, responseData);
  }

  /**
   * GetUser retrieves a specific user together with their primary account context.
   */
  async getUser(request: GetUserRequestData): Promise<GetUserResponse> {
    const headers: Record<string, string> = {
      'Content-Type': 'application/json',
    };

    if (this.authorization) {
      headers['Authorization'] = this.authorization;
    }

    const requestMessage = create(GetUserRequestSchema, request || {});
    const response = await this.fetch(`${this.endpoint}/GetUser`, {
      method: 'POST',
      headers,
      body: JSON.stringify(toJson(GetUserRequestSchema, requestMessage)),
    });

    if (!response.ok) {
      const error = await response.text();
      throw new Error(`GetUser failed: ${response.status} ${error}`);
    }

    const responseData = (await response.json()) as any;
    return fromJson(GetUserResponseSchema, responseData);
  }

  /**
   * UpdateUser updates user properties. Optionally renames the user's primary account.
   */
  async updateUser(request: UpdateUserRequestData): Promise<UpdateUserResponse> {
    const headers: Record<string, string> = {
      'Content-Type': 'application/json',
    };

    if (this.authorization) {
      headers['Authorization'] = this.authorization;
    }

    const requestMessage = create(UpdateUserRequestSchema, request || {});
    const response = await this.fetch(`${this.endpoint}/UpdateUser`, {
      method: 'POST',
      headers,
      body: JSON.stringify(toJson(UpdateUserRequestSchema, requestMessage)),
    });

    if (!response.ok) {
      const error = await response.text();
      throw new Error(`UpdateUser failed: ${response.status} ${error}`);
    }

    const responseData = (await response.json()) as any;
    return fromJson(UpdateUserResponseSchema, responseData);
  }

  /**
   * DeleteUser soft-deletes a user. Set purge_data=true to also destroy the primary account and all mailbox data.
   */
  async deleteUser(request: DeleteUserRequestData): Promise<DeleteUserResponse> {
    const headers: Record<string, string> = {
      'Content-Type': 'application/json',
    };

    if (this.authorization) {
      headers['Authorization'] = this.authorization;
    }

    const requestMessage = create(DeleteUserRequestSchema, request || {});
    const response = await this.fetch(`${this.endpoint}/DeleteUser`, {
      method: 'POST',
      headers,
      body: JSON.stringify(toJson(DeleteUserRequestSchema, requestMessage)),
    });

    if (!response.ok) {
      const error = await response.text();
      throw new Error(`DeleteUser failed: ${response.status} ${error}`);
    }

    const responseData = (await response.json()) as any;
    return fromJson(DeleteUserResponseSchema, responseData);
  }

  /**
   * AddUserAccountCapability idempotently adds a new JMAP capability to the user's primary account and provisions the required resources.
   */
  async addUserAccountCapability(request: AddUserAccountCapabilityRequestData): Promise<AddUserAccountCapabilityResponse> {
    const headers: Record<string, string> = {
      'Content-Type': 'application/json',
    };

    if (this.authorization) {
      headers['Authorization'] = this.authorization;
    }

    const requestMessage = create(AddUserAccountCapabilityRequestSchema, request || {});
    const response = await this.fetch(`${this.endpoint}/AddUserAccountCapability`, {
      method: 'POST',
      headers,
      body: JSON.stringify(toJson(AddUserAccountCapabilityRequestSchema, requestMessage)),
    });

    if (!response.ok) {
      const error = await response.text();
      throw new Error(`AddUserAccountCapability failed: ${response.status} ${error}`);
    }

    const responseData = (await response.json()) as any;
    return fromJson(AddUserAccountCapabilityResponseSchema, responseData);
  }

}

// Re-export types from protobuf
export * from './users_pb';
