// Code generated by protoc-gen-ts-client. DO NOT EDIT.

import type { Message } from '@bufbuild/protobuf';
import { create, toJson, fromJson } from '@bufbuild/protobuf';

import type {
  GetReadinessRequest,
  GetReadinessResponse,
  DismissWelcomeRequest,
  DismissWelcomeResponse,
} from './setup_pb';

import {
  GetReadinessRequestSchema,
  GetReadinessResponseSchema,
  DismissWelcomeRequestSchema,
  DismissWelcomeResponseSchema,
} from './setup_pb';

// Request types without protobuf Message metadata
type GetReadinessRequestData = Omit<GetReadinessRequest, keyof Message>;
type DismissWelcomeRequestData = Omit<DismissWelcomeRequest, keyof Message>;

export interface SetupServiceClientConfig {
  endpoint: string;
  authorization?: string;
  /** Custom fetch function for request interceptors, token refresh, retry logic, etc. */
  fetch?: typeof fetch;
}

/**
 * SetupService client - AWS SDK-style client for SetupService
 */
export class SetupServiceClient {
  private readonly endpoint: string;
  private readonly authorization?: string;
  private readonly fetch: typeof fetch;

  constructor(config: SetupServiceClientConfig) {
    this.endpoint = config.endpoint.replace(/\/$/, '');
    this.authorization = config.authorization;
    this.fetch = config.fetch || globalThis.fetch.bind(globalThis);
  }

  /**
   * GetReadiness returns the aggregated onboarding/health snapshot. Read-only.
   */
  async getReadiness(request?: GetReadinessRequestData): Promise<GetReadinessResponse> {
    const headers: Record<string, string> = {
      'Content-Type': 'application/json',
    };

    if (this.authorization) {
      headers['Authorization'] = this.authorization;
    }

    const requestMessage = create(GetReadinessRequestSchema, request || {});
    const response = await this.fetch(`${this.endpoint}/GetReadiness`, {
      method: 'POST',
      headers,
      body: JSON.stringify(toJson(GetReadinessRequestSchema, requestMessage)),
    });

    if (!response.ok) {
      const error = await response.text();
      throw new Error(`GetReadiness failed: ${response.status} ${error}`);
    }

    const responseData = (await response.json()) as any;
    return fromJson(GetReadinessResponseSchema, responseData);
  }

  /**
   * DismissWelcome persists the deployment-wide Welcome graduation flag.
   */
  async dismissWelcome(request: DismissWelcomeRequestData): Promise<DismissWelcomeResponse> {
    const headers: Record<string, string> = {
      'Content-Type': 'application/json',
    };

    if (this.authorization) {
      headers['Authorization'] = this.authorization;
    }

    const requestMessage = create(DismissWelcomeRequestSchema, request || {});
    const response = await this.fetch(`${this.endpoint}/DismissWelcome`, {
      method: 'POST',
      headers,
      body: JSON.stringify(toJson(DismissWelcomeRequestSchema, requestMessage)),
    });

    if (!response.ok) {
      const error = await response.text();
      throw new Error(`DismissWelcome failed: ${response.status} ${error}`);
    }

    const responseData = (await response.json()) as any;
    return fromJson(DismissWelcomeResponseSchema, responseData);
  }

}

// Re-export types from protobuf
export * from './setup_pb';
