// Code generated by protoc-gen-ts-client. DO NOT EDIT.

import type { Message } from '@bufbuild/protobuf';
import { create, toJson, fromJson } from '@bufbuild/protobuf';

import type {
  ListAuditLogsRequest,
  ListAuditLogsResponse,
  ListActorsRequest,
  ListActorsResponse,
  ListOperationsRequest,
  ListOperationsResponse,
} from './audit_pb';

import {
  ListAuditLogsRequestSchema,
  ListAuditLogsResponseSchema,
  ListActorsRequestSchema,
  ListActorsResponseSchema,
  ListOperationsRequestSchema,
  ListOperationsResponseSchema,
} from './audit_pb';

// Request types without protobuf Message metadata
type ListAuditLogsRequestData = Omit<ListAuditLogsRequest, keyof Message>;
type ListActorsRequestData = Omit<ListActorsRequest, keyof Message>;
type ListOperationsRequestData = Omit<ListOperationsRequest, keyof Message>;

export interface AuditServiceClientConfig {
  endpoint: string;
  authorization?: string;
  /** Custom fetch function for request interceptors, token refresh, retry logic, etc. */
  fetch?: typeof fetch;
}

/**
 * AuditService provides access to audit logs for admin operations
 */
export class AuditServiceClient {
  private readonly endpoint: string;
  private readonly authorization?: string;
  private readonly fetch: typeof fetch;

  constructor(config: AuditServiceClientConfig) {
    this.endpoint = config.endpoint.replace(/\/$/, '');
    this.authorization = config.authorization;
    this.fetch = config.fetch || globalThis.fetch.bind(globalThis);
  }

  /**
   * ListAuditLogs retrieves audit logs with optional filtering
   * Filters use AND logic: all provided filters must match
   * No filters = timeline (all logs, most efficient)
   * Operation filter only = uses GSI2 index (efficient)
   * Actor filters = both actor_type and actor_id required (uses GSI3, efficient)
   * Both operation and actor = uses GSI2 + client-side actor filtering (less efficient)
   */
  async listAuditLogs(request?: ListAuditLogsRequestData): Promise<ListAuditLogsResponse> {
    const headers: Record<string, string> = {
      'Content-Type': 'application/json',
    };

    if (this.authorization) {
      headers['Authorization'] = this.authorization;
    }

    const requestMessage = create(ListAuditLogsRequestSchema, request || {});
    const response = await this.fetch(`${this.endpoint}/ListAuditLogs`, {
      method: 'POST',
      headers,
      body: JSON.stringify(toJson(ListAuditLogsRequestSchema, requestMessage)),
    });

    if (!response.ok) {
      const error = await response.text();
      throw new Error(`ListAuditLogs failed: ${response.status} ${error}`);
    }

    const responseData = (await response.json()) as any;
    return fromJson(ListAuditLogsResponseSchema, responseData);
  }

  /**
   * ListActors returns all actors of a specific type with pagination
   */
  async listActors(request: ListActorsRequestData): Promise<ListActorsResponse> {
    const headers: Record<string, string> = {
      'Content-Type': 'application/json',
    };

    if (this.authorization) {
      headers['Authorization'] = this.authorization;
    }

    const requestMessage = create(ListActorsRequestSchema, request || {});
    const response = await this.fetch(`${this.endpoint}/ListActors`, {
      method: 'POST',
      headers,
      body: JSON.stringify(toJson(ListActorsRequestSchema, requestMessage)),
    });

    if (!response.ok) {
      const error = await response.text();
      throw new Error(`ListActors failed: ${response.status} ${error}`);
    }

    const responseData = (await response.json()) as any;
    return fromJson(ListActorsResponseSchema, responseData);
  }

  /**
   * ListOperations returns all available operations that have audit logs
   */
  async listOperations(request?: ListOperationsRequestData): Promise<ListOperationsResponse> {
    const headers: Record<string, string> = {
      'Content-Type': 'application/json',
    };

    if (this.authorization) {
      headers['Authorization'] = this.authorization;
    }

    const requestMessage = create(ListOperationsRequestSchema, request || {});
    const response = await this.fetch(`${this.endpoint}/ListOperations`, {
      method: 'POST',
      headers,
      body: JSON.stringify(toJson(ListOperationsRequestSchema, requestMessage)),
    });

    if (!response.ok) {
      const error = await response.text();
      throw new Error(`ListOperations failed: ${response.status} ${error}`);
    }

    const responseData = (await response.json()) as any;
    return fromJson(ListOperationsResponseSchema, responseData);
  }

}

// Re-export types from protobuf
export * from './audit_pb';
