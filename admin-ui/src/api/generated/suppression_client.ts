// Code generated by protoc-gen-ts-client. DO NOT EDIT.

import type { Message } from '@bufbuild/protobuf';
import { create, toJson, fromJson } from '@bufbuild/protobuf';

import type {
  GetSuppressionRequest,
  GetSuppressionResponse,
  ListSuppressionsRequest,
  ListSuppressionsResponse,
  ClearBounceSuppressionRequest,
  ClearBounceSuppressionResponse,
  ClearComplaintSuppressionRequest,
  ClearComplaintSuppressionResponse,
} from './suppression_pb';

import {
  GetSuppressionRequestSchema,
  GetSuppressionResponseSchema,
  ListSuppressionsRequestSchema,
  ListSuppressionsResponseSchema,
  ClearBounceSuppressionRequestSchema,
  ClearBounceSuppressionResponseSchema,
  ClearComplaintSuppressionRequestSchema,
  ClearComplaintSuppressionResponseSchema,
} from './suppression_pb';

// Request types without protobuf Message metadata
type GetSuppressionRequestData = Omit<GetSuppressionRequest, keyof Message>;
type ListSuppressionsRequestData = Omit<ListSuppressionsRequest, keyof Message>;
type ClearBounceSuppressionRequestData = Omit<ClearBounceSuppressionRequest, keyof Message>;
type ClearComplaintSuppressionRequestData = Omit<ClearComplaintSuppressionRequest, keyof Message>;

export interface SuppressionServiceClientConfig {
  endpoint: string;
  authorization?: string;
  /** Custom fetch function for request interceptors, token refresh, retry logic, etc. */
  fetch?: typeof fetch;
}

/**
 * SuppressionService provides operations for managing recipient suppressions
 */
export class SuppressionServiceClient {
  private readonly endpoint: string;
  private readonly authorization?: string;
  private readonly fetch: typeof fetch;

  constructor(config: SuppressionServiceClientConfig) {
    this.endpoint = config.endpoint.replace(/\/$/, '');
    this.authorization = config.authorization;
    this.fetch = config.fetch || globalThis.fetch.bind(globalThis);
  }

  /**
   * GetSuppression retrieves suppression details for a specific recipient
   */
  async getSuppression(request: GetSuppressionRequestData): Promise<GetSuppressionResponse> {
    const headers: Record<string, string> = {
      'Content-Type': 'application/json',
    };

    if (this.authorization) {
      headers['Authorization'] = this.authorization;
    }

    const requestMessage = create(GetSuppressionRequestSchema, request || {});
    const response = await this.fetch(`${this.endpoint}/GetSuppression`, {
      method: 'POST',
      headers,
      body: JSON.stringify(toJson(GetSuppressionRequestSchema, requestMessage)),
    });

    if (!response.ok) {
      const error = await response.text();
      throw new Error(`GetSuppression failed: ${response.status} ${error}`);
    }

    const responseData = (await response.json()) as any;
    return fromJson(GetSuppressionResponseSchema, responseData);
  }

  /**
   * ListSuppressions lists all suppressions with pagination
   */
  async listSuppressions(request?: ListSuppressionsRequestData): Promise<ListSuppressionsResponse> {
    const headers: Record<string, string> = {
      'Content-Type': 'application/json',
    };

    if (this.authorization) {
      headers['Authorization'] = this.authorization;
    }

    const requestMessage = create(ListSuppressionsRequestSchema, request || {});
    const response = await this.fetch(`${this.endpoint}/ListSuppressions`, {
      method: 'POST',
      headers,
      body: JSON.stringify(toJson(ListSuppressionsRequestSchema, requestMessage)),
    });

    if (!response.ok) {
      const error = await response.text();
      throw new Error(`ListSuppressions failed: ${response.status} ${error}`);
    }

    const responseData = (await response.json()) as any;
    return fromJson(ListSuppressionsResponseSchema, responseData);
  }

  /**
   * ClearBounceSuppression clears bounce suppression (sets level to NONE) while preserving counters/history.
   */
  async clearBounceSuppression(request: ClearBounceSuppressionRequestData): Promise<ClearBounceSuppressionResponse> {
    const headers: Record<string, string> = {
      'Content-Type': 'application/json',
    };

    if (this.authorization) {
      headers['Authorization'] = this.authorization;
    }

    const requestMessage = create(ClearBounceSuppressionRequestSchema, request || {});
    const response = await this.fetch(`${this.endpoint}/ClearBounceSuppression`, {
      method: 'POST',
      headers,
      body: JSON.stringify(toJson(ClearBounceSuppressionRequestSchema, requestMessage)),
    });

    if (!response.ok) {
      const error = await response.text();
      throw new Error(`ClearBounceSuppression failed: ${response.status} ${error}`);
    }

    const responseData = (await response.json()) as any;
    return fromJson(ClearBounceSuppressionResponseSchema, responseData);
  }

  /**
   * ClearComplaintSuppression clears complaint suppression for a specific message class (sets level to NONE)
   * while preserving counters/history.
   */
  async clearComplaintSuppression(request: ClearComplaintSuppressionRequestData): Promise<ClearComplaintSuppressionResponse> {
    const headers: Record<string, string> = {
      'Content-Type': 'application/json',
    };

    if (this.authorization) {
      headers['Authorization'] = this.authorization;
    }

    const requestMessage = create(ClearComplaintSuppressionRequestSchema, request || {});
    const response = await this.fetch(`${this.endpoint}/ClearComplaintSuppression`, {
      method: 'POST',
      headers,
      body: JSON.stringify(toJson(ClearComplaintSuppressionRequestSchema, requestMessage)),
    });

    if (!response.ok) {
      const error = await response.text();
      throw new Error(`ClearComplaintSuppression failed: ${response.status} ${error}`);
    }

    const responseData = (await response.json()) as any;
    return fromJson(ClearComplaintSuppressionResponseSchema, responseData);
  }

}

// Re-export types from protobuf
export * from './suppression_pb';
