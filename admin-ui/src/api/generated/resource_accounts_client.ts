// Code generated by protoc-gen-ts-client. DO NOT EDIT.

import type { Message } from '@bufbuild/protobuf';
import { create, toJson, fromJson } from '@bufbuild/protobuf';

import type {
  CreateResourceAccountRequest,
  CreateResourceAccountResponse,
  GetResourceAccountRequest,
  GetResourceAccountResponse,
  ListResourceAccountsRequest,
  ListResourceAccountsResponse,
  UpdateResourceAccountRequest,
  UpdateResourceAccountResponse,
  DeleteResourceAccountRequest,
  DeleteResourceAccountResponse,
  AddDelegateRequest,
  AddDelegateResponse,
  RemoveDelegateRequest,
  RemoveDelegateResponse,
  ListDelegatesRequest,
  ListDelegatesResponse,
  AddResourceAccountCapabilityRequest,
  AddResourceAccountCapabilityResponse,
} from './resource_accounts_pb';

import {
  CreateResourceAccountRequestSchema,
  CreateResourceAccountResponseSchema,
  GetResourceAccountRequestSchema,
  GetResourceAccountResponseSchema,
  ListResourceAccountsRequestSchema,
  ListResourceAccountsResponseSchema,
  UpdateResourceAccountRequestSchema,
  UpdateResourceAccountResponseSchema,
  DeleteResourceAccountRequestSchema,
  DeleteResourceAccountResponseSchema,
  AddDelegateRequestSchema,
  AddDelegateResponseSchema,
  RemoveDelegateRequestSchema,
  RemoveDelegateResponseSchema,
  ListDelegatesRequestSchema,
  ListDelegatesResponseSchema,
  AddResourceAccountCapabilityRequestSchema,
  AddResourceAccountCapabilityResponseSchema,
} from './resource_accounts_pb';

// Request types without protobuf Message metadata
type CreateResourceAccountRequestData = Omit<CreateResourceAccountRequest, keyof Message>;
type GetResourceAccountRequestData = Omit<GetResourceAccountRequest, keyof Message>;
type ListResourceAccountsRequestData = Omit<ListResourceAccountsRequest, keyof Message>;
type UpdateResourceAccountRequestData = Omit<UpdateResourceAccountRequest, keyof Message>;
type DeleteResourceAccountRequestData = Omit<DeleteResourceAccountRequest, keyof Message>;
type AddDelegateRequestData = Omit<AddDelegateRequest, keyof Message>;
type RemoveDelegateRequestData = Omit<RemoveDelegateRequest, keyof Message>;
type ListDelegatesRequestData = Omit<ListDelegatesRequest, keyof Message>;
type AddResourceAccountCapabilityRequestData = Omit<AddResourceAccountCapabilityRequest, keyof Message>;

export interface ResourceAccountsServiceClientConfig {
  endpoint: string;
  authorization?: string;
  /** Custom fetch function for request interceptors, token refresh, retry logic, etc. */
  fetch?: typeof fetch;
}

/**
 * ResourceAccountsService manages resource account principals and their delegates.
 * A resource account is a resource mailbox (mailbox, room, or equipment) that can be
 * accessed by multiple users via delegation.
 */
export class ResourceAccountsServiceClient {
  private readonly endpoint: string;
  private readonly authorization?: string;
  private readonly fetch: typeof fetch;

  constructor(config: ResourceAccountsServiceClientConfig) {
    this.endpoint = config.endpoint.replace(/\/$/, '');
    this.authorization = config.authorization;
    this.fetch = config.fetch || globalThis.fetch.bind(globalThis);
  }

  /**
   * CreateResourceAccount provisions a new resource account with the given identity.
   */
  async createResourceAccount(request: CreateResourceAccountRequestData): Promise<CreateResourceAccountResponse> {
    const headers: Record<string, string> = {
      'Content-Type': 'application/json',
    };

    if (this.authorization) {
      headers['Authorization'] = this.authorization;
    }

    const requestMessage = create(CreateResourceAccountRequestSchema, request || {});
    const response = await this.fetch(`${this.endpoint}/CreateResourceAccount`, {
      method: 'POST',
      headers,
      body: JSON.stringify(toJson(CreateResourceAccountRequestSchema, requestMessage)),
    });

    if (!response.ok) {
      const error = await response.text();
      throw new Error(`CreateResourceAccount failed: ${response.status} ${error}`);
    }

    const responseData = (await response.json()) as any;
    return fromJson(CreateResourceAccountResponseSchema, responseData);
  }

  /**
   * GetResourceAccount retrieves a resource account by ID.
   */
  async getResourceAccount(request: GetResourceAccountRequestData): Promise<GetResourceAccountResponse> {
    const headers: Record<string, string> = {
      'Content-Type': 'application/json',
    };

    if (this.authorization) {
      headers['Authorization'] = this.authorization;
    }

    const requestMessage = create(GetResourceAccountRequestSchema, request || {});
    const response = await this.fetch(`${this.endpoint}/GetResourceAccount`, {
      method: 'POST',
      headers,
      body: JSON.stringify(toJson(GetResourceAccountRequestSchema, requestMessage)),
    });

    if (!response.ok) {
      const error = await response.text();
      throw new Error(`GetResourceAccount failed: ${response.status} ${error}`);
    }

    const responseData = (await response.json()) as any;
    return fromJson(GetResourceAccountResponseSchema, responseData);
  }

  /**
   * ListResourceAccounts lists all resource accounts for the tenant.
   */
  async listResourceAccounts(request?: ListResourceAccountsRequestData): Promise<ListResourceAccountsResponse> {
    const headers: Record<string, string> = {
      'Content-Type': 'application/json',
    };

    if (this.authorization) {
      headers['Authorization'] = this.authorization;
    }

    const requestMessage = create(ListResourceAccountsRequestSchema, request || {});
    const response = await this.fetch(`${this.endpoint}/ListResourceAccounts`, {
      method: 'POST',
      headers,
      body: JSON.stringify(toJson(ListResourceAccountsRequestSchema, requestMessage)),
    });

    if (!response.ok) {
      const error = await response.text();
      throw new Error(`ListResourceAccounts failed: ${response.status} ${error}`);
    }

    const responseData = (await response.json()) as any;
    return fromJson(ListResourceAccountsResponseSchema, responseData);
  }

  /**
   * UpdateResourceAccount updates resource account properties.
   */
  async updateResourceAccount(request: UpdateResourceAccountRequestData): Promise<UpdateResourceAccountResponse> {
    const headers: Record<string, string> = {
      'Content-Type': 'application/json',
    };

    if (this.authorization) {
      headers['Authorization'] = this.authorization;
    }

    const requestMessage = create(UpdateResourceAccountRequestSchema, request || {});
    const response = await this.fetch(`${this.endpoint}/UpdateResourceAccount`, {
      method: 'POST',
      headers,
      body: JSON.stringify(toJson(UpdateResourceAccountRequestSchema, requestMessage)),
    });

    if (!response.ok) {
      const error = await response.text();
      throw new Error(`UpdateResourceAccount failed: ${response.status} ${error}`);
    }

    const responseData = (await response.json()) as any;
    return fromJson(UpdateResourceAccountResponseSchema, responseData);
  }

  /**
   * DeleteResourceAccount removes the resource account and its account.
   */
  async deleteResourceAccount(request: DeleteResourceAccountRequestData): Promise<DeleteResourceAccountResponse> {
    const headers: Record<string, string> = {
      'Content-Type': 'application/json',
    };

    if (this.authorization) {
      headers['Authorization'] = this.authorization;
    }

    const requestMessage = create(DeleteResourceAccountRequestSchema, request || {});
    const response = await this.fetch(`${this.endpoint}/DeleteResourceAccount`, {
      method: 'POST',
      headers,
      body: JSON.stringify(toJson(DeleteResourceAccountRequestSchema, requestMessage)),
    });

    if (!response.ok) {
      const error = await response.text();
      throw new Error(`DeleteResourceAccount failed: ${response.status} ${error}`);
    }

    const responseData = (await response.json()) as any;
    return fromJson(DeleteResourceAccountResponseSchema, responseData);
  }

  /**
   * AddDelegate grants a principal delegate access to a resource account.
   */
  async addDelegate(request: AddDelegateRequestData): Promise<AddDelegateResponse> {
    const headers: Record<string, string> = {
      'Content-Type': 'application/json',
    };

    if (this.authorization) {
      headers['Authorization'] = this.authorization;
    }

    const requestMessage = create(AddDelegateRequestSchema, request || {});
    const response = await this.fetch(`${this.endpoint}/AddDelegate`, {
      method: 'POST',
      headers,
      body: JSON.stringify(toJson(AddDelegateRequestSchema, requestMessage)),
    });

    if (!response.ok) {
      const error = await response.text();
      throw new Error(`AddDelegate failed: ${response.status} ${error}`);
    }

    const responseData = (await response.json()) as any;
    return fromJson(AddDelegateResponseSchema, responseData);
  }

  /**
   * RemoveDelegate revokes delegate access from a principal.
   */
  async removeDelegate(request: RemoveDelegateRequestData): Promise<RemoveDelegateResponse> {
    const headers: Record<string, string> = {
      'Content-Type': 'application/json',
    };

    if (this.authorization) {
      headers['Authorization'] = this.authorization;
    }

    const requestMessage = create(RemoveDelegateRequestSchema, request || {});
    const response = await this.fetch(`${this.endpoint}/RemoveDelegate`, {
      method: 'POST',
      headers,
      body: JSON.stringify(toJson(RemoveDelegateRequestSchema, requestMessage)),
    });

    if (!response.ok) {
      const error = await response.text();
      throw new Error(`RemoveDelegate failed: ${response.status} ${error}`);
    }

    const responseData = (await response.json()) as any;
    return fromJson(RemoveDelegateResponseSchema, responseData);
  }

  /**
   * ListDelegates lists all delegates for a resource account.
   */
  async listDelegates(request: ListDelegatesRequestData): Promise<ListDelegatesResponse> {
    const headers: Record<string, string> = {
      'Content-Type': 'application/json',
    };

    if (this.authorization) {
      headers['Authorization'] = this.authorization;
    }

    const requestMessage = create(ListDelegatesRequestSchema, request || {});
    const response = await this.fetch(`${this.endpoint}/ListDelegates`, {
      method: 'POST',
      headers,
      body: JSON.stringify(toJson(ListDelegatesRequestSchema, requestMessage)),
    });

    if (!response.ok) {
      const error = await response.text();
      throw new Error(`ListDelegates failed: ${response.status} ${error}`);
    }

    const responseData = (await response.json()) as any;
    return fromJson(ListDelegatesResponseSchema, responseData);
  }

  /**
   * AddResourceAccountCapability idempotently adds a new JMAP capability to a resource account and provisions the required resources.
   */
  async addResourceAccountCapability(request: AddResourceAccountCapabilityRequestData): Promise<AddResourceAccountCapabilityResponse> {
    const headers: Record<string, string> = {
      'Content-Type': 'application/json',
    };

    if (this.authorization) {
      headers['Authorization'] = this.authorization;
    }

    const requestMessage = create(AddResourceAccountCapabilityRequestSchema, request || {});
    const response = await this.fetch(`${this.endpoint}/AddResourceAccountCapability`, {
      method: 'POST',
      headers,
      body: JSON.stringify(toJson(AddResourceAccountCapabilityRequestSchema, requestMessage)),
    });

    if (!response.ok) {
      const error = await response.text();
      throw new Error(`AddResourceAccountCapability failed: ${response.status} ${error}`);
    }

    const responseData = (await response.json()) as any;
    return fromJson(AddResourceAccountCapabilityResponseSchema, responseData);
  }

}

// Re-export types from protobuf
export * from './resource_accounts_pb';
