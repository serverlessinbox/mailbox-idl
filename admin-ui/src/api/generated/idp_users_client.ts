// Code generated by protoc-gen-ts-client. DO NOT EDIT.

import type { Message } from '@bufbuild/protobuf';
import { create, toJson, fromJson } from '@bufbuild/protobuf';

import type {
  ListIdpUsersRequest,
  ListIdpUsersResponse,
  GetIdpUserRequest,
  GetIdpUserResponse,
  CreateIdpUserRequest,
  CreateIdpUserResponse,
  SetIdpUserEnabledRequest,
  SetIdpUserEnabledResponse,
  SetIdpUserPasswordRequest,
  SetIdpUserPasswordResponse,
} from './idp_users_pb';

import {
  ListIdpUsersRequestSchema,
  ListIdpUsersResponseSchema,
  GetIdpUserRequestSchema,
  GetIdpUserResponseSchema,
  CreateIdpUserRequestSchema,
  CreateIdpUserResponseSchema,
  SetIdpUserEnabledRequestSchema,
  SetIdpUserEnabledResponseSchema,
  SetIdpUserPasswordRequestSchema,
  SetIdpUserPasswordResponseSchema,
} from './idp_users_pb';

// Request types without protobuf Message metadata
type ListIdpUsersRequestData = Omit<ListIdpUsersRequest, keyof Message>;
type GetIdpUserRequestData = Omit<GetIdpUserRequest, keyof Message>;
type CreateIdpUserRequestData = Omit<CreateIdpUserRequest, keyof Message>;
type SetIdpUserEnabledRequestData = Omit<SetIdpUserEnabledRequest, keyof Message>;
type SetIdpUserPasswordRequestData = Omit<SetIdpUserPasswordRequest, keyof Message>;

export interface IdpUsersServiceClientConfig {
  endpoint: string;
  authorization?: string;
  /** Custom fetch function for request interceptors, token refresh, retry logic, etc. */
  fetch?: typeof fetch;
}

/**
 * IdpUsersService provides operations for managing IdP users (provider-specific).
 */
export class IdpUsersServiceClient {
  private readonly endpoint: string;
  private readonly authorization?: string;
  private readonly fetch: typeof fetch;

  constructor(config: IdpUsersServiceClientConfig) {
    this.endpoint = config.endpoint.replace(/\/$/, '');
    this.authorization = config.authorization;
    this.fetch = config.fetch || globalThis.fetch.bind(globalThis);
  }

  /**
   * ListIdpUsers retrieves IdP users with pagination.
   */
  async listIdpUsers(request?: ListIdpUsersRequestData): Promise<ListIdpUsersResponse> {
    const headers: Record<string, string> = {
      'Content-Type': 'application/json',
    };

    if (this.authorization) {
      headers['Authorization'] = this.authorization;
    }

    const requestMessage = create(ListIdpUsersRequestSchema, request || {});
    const response = await this.fetch(`${this.endpoint}/ListIdpUsers`, {
      method: 'POST',
      headers,
      body: JSON.stringify(toJson(ListIdpUsersRequestSchema, requestMessage)),
    });

    if (!response.ok) {
      const error = await response.text();
      throw new Error(`ListIdpUsers failed: ${response.status} ${error}`);
    }

    const responseData = (await response.json()) as any;
    return fromJson(ListIdpUsersResponseSchema, responseData);
  }

  /**
   * GetIdpUser retrieves a specific IdP user by username.
   */
  async getIdpUser(request: GetIdpUserRequestData): Promise<GetIdpUserResponse> {
    const headers: Record<string, string> = {
      'Content-Type': 'application/json',
    };

    if (this.authorization) {
      headers['Authorization'] = this.authorization;
    }

    const requestMessage = create(GetIdpUserRequestSchema, request || {});
    const response = await this.fetch(`${this.endpoint}/GetIdpUser`, {
      method: 'POST',
      headers,
      body: JSON.stringify(toJson(GetIdpUserRequestSchema, requestMessage)),
    });

    if (!response.ok) {
      const error = await response.text();
      throw new Error(`GetIdpUser failed: ${response.status} ${error}`);
    }

    const responseData = (await response.json()) as any;
    return fromJson(GetIdpUserResponseSchema, responseData);
  }

  /**
   * CreateIdpUser creates a new IdP user (admin-driven onboarding).
   */
  async createIdpUser(request: CreateIdpUserRequestData): Promise<CreateIdpUserResponse> {
    const headers: Record<string, string> = {
      'Content-Type': 'application/json',
    };

    if (this.authorization) {
      headers['Authorization'] = this.authorization;
    }

    const requestMessage = create(CreateIdpUserRequestSchema, request || {});
    const response = await this.fetch(`${this.endpoint}/CreateIdpUser`, {
      method: 'POST',
      headers,
      body: JSON.stringify(toJson(CreateIdpUserRequestSchema, requestMessage)),
    });

    if (!response.ok) {
      const error = await response.text();
      throw new Error(`CreateIdpUser failed: ${response.status} ${error}`);
    }

    const responseData = (await response.json()) as any;
    return fromJson(CreateIdpUserResponseSchema, responseData);
  }

  /**
   * SetIdpUserEnabled enables/disables an IdP user.
   */
  async setIdpUserEnabled(request: SetIdpUserEnabledRequestData): Promise<SetIdpUserEnabledResponse> {
    const headers: Record<string, string> = {
      'Content-Type': 'application/json',
    };

    if (this.authorization) {
      headers['Authorization'] = this.authorization;
    }

    const requestMessage = create(SetIdpUserEnabledRequestSchema, request || {});
    const response = await this.fetch(`${this.endpoint}/SetIdpUserEnabled`, {
      method: 'POST',
      headers,
      body: JSON.stringify(toJson(SetIdpUserEnabledRequestSchema, requestMessage)),
    });

    if (!response.ok) {
      const error = await response.text();
      throw new Error(`SetIdpUserEnabled failed: ${response.status} ${error}`);
    }

    const responseData = (await response.json()) as any;
    return fromJson(SetIdpUserEnabledResponseSchema, responseData);
  }

  /**
   * SetIdpUserPassword sets a user's password (admin-driven; avoids email dependency).
   */
  async setIdpUserPassword(request: SetIdpUserPasswordRequestData): Promise<SetIdpUserPasswordResponse> {
    const headers: Record<string, string> = {
      'Content-Type': 'application/json',
    };

    if (this.authorization) {
      headers['Authorization'] = this.authorization;
    }

    const requestMessage = create(SetIdpUserPasswordRequestSchema, request || {});
    const response = await this.fetch(`${this.endpoint}/SetIdpUserPassword`, {
      method: 'POST',
      headers,
      body: JSON.stringify(toJson(SetIdpUserPasswordRequestSchema, requestMessage)),
    });

    if (!response.ok) {
      const error = await response.text();
      throw new Error(`SetIdpUserPassword failed: ${response.status} ${error}`);
    }

    const responseData = (await response.json()) as any;
    return fromJson(SetIdpUserPasswordResponseSchema, responseData);
  }

}

// Re-export types from protobuf
export * from './idp_users_pb';
