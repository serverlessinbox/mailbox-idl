// Code generated by protoc-gen-ts-client. DO NOT EDIT.

import type { Message } from '@bufbuild/protobuf';
import { create, toJson, fromJson } from '@bufbuild/protobuf';

import type {
  GetStatusRequest,
  GetStatusResponse,
} from './status_pb';

import {
  GetStatusRequestSchema,
  GetStatusResponseSchema,
} from './status_pb';

// Request types without protobuf Message metadata
type GetStatusRequestData = Omit<GetStatusRequest, keyof Message>;

export interface StatusServiceClientConfig {
  endpoint: string;
  authorization?: string;
  /** Custom fetch function for request interceptors, token refresh, retry logic, etc. */
  fetch?: typeof fetch;
}

/**
 * StatusService client - AWS SDK-style client for StatusService
 */
export class StatusServiceClient {
  private readonly endpoint: string;
  private readonly authorization?: string;
  private readonly fetch: typeof fetch;

  constructor(config: StatusServiceClientConfig) {
    this.endpoint = config.endpoint.replace(/\/$/, '');
    this.authorization = config.authorization;
    this.fetch = config.fetch || globalThis.fetch.bind(globalThis);
  }

  /**
   * GetStatus returns a snapshot of SES sandbox status + operational hints.
   */
  async getStatus(request?: GetStatusRequestData): Promise<GetStatusResponse> {
    const headers: Record<string, string> = {
      'Content-Type': 'application/json',
    };

    if (this.authorization) {
      headers['Authorization'] = this.authorization;
    }

    const requestMessage = create(GetStatusRequestSchema, request || {});
    const response = await this.fetch(`${this.endpoint}/GetStatus`, {
      method: 'POST',
      headers,
      body: JSON.stringify(toJson(GetStatusRequestSchema, requestMessage)),
    });

    if (!response.ok) {
      const error = await response.text();
      throw new Error(`GetStatus failed: ${response.status} ${error}`);
    }

    const responseData = (await response.json()) as any;
    return fromJson(GetStatusResponseSchema, responseData);
  }

}

// Re-export types from protobuf
export * from './status_pb';
