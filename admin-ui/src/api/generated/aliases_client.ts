// Code generated by protoc-gen-ts-client. DO NOT EDIT.

import type { Message } from '@bufbuild/protobuf';
import { create, toJson, fromJson } from '@bufbuild/protobuf';

import type {
  ListAccountAliasesRequest,
  ListAccountAliasesResponse,
  CreateAliasRequest,
  CreateAliasResponse,
  UpdateAliasRequest,
  UpdateAliasResponse,
  DeleteAliasRequest,
  DeleteAliasResponse,
} from './aliases_pb';

import {
  ListAccountAliasesRequestSchema,
  ListAccountAliasesResponseSchema,
  CreateAliasRequestSchema,
  CreateAliasResponseSchema,
  UpdateAliasRequestSchema,
  UpdateAliasResponseSchema,
  DeleteAliasRequestSchema,
  DeleteAliasResponseSchema,
} from './aliases_pb';

// Request types without protobuf Message metadata
type ListAccountAliasesRequestData = Omit<ListAccountAliasesRequest, keyof Message>;
type CreateAliasRequestData = Omit<CreateAliasRequest, keyof Message>;
type UpdateAliasRequestData = Omit<UpdateAliasRequest, keyof Message>;
type DeleteAliasRequestData = Omit<DeleteAliasRequest, keyof Message>;

export interface AliasesServiceClientConfig {
  endpoint: string;
  authorization?: string;
  /** Custom fetch function for request interceptors, token refresh, retry logic, etc. */
  fetch?: typeof fetch;
}

/**
 * AliasesService provides operations for managing aliases.
 */
export class AliasesServiceClient {
  private readonly endpoint: string;
  private readonly authorization?: string;
  private readonly fetch: typeof fetch;

  constructor(config: AliasesServiceClientConfig) {
    this.endpoint = config.endpoint.replace(/\/$/, '');
    this.authorization = config.authorization;
    this.fetch = config.fetch || globalThis.fetch.bind(globalThis);
  }

  /**
   * ListAccountAliases lists all aliases (receiving addresses) for an account.
   */
  async listAccountAliases(request: ListAccountAliasesRequestData): Promise<ListAccountAliasesResponse> {
    const headers: Record<string, string> = {
      'Content-Type': 'application/json',
    };

    if (this.authorization) {
      headers['Authorization'] = this.authorization;
    }

    const requestMessage = create(ListAccountAliasesRequestSchema, request || {});
    const response = await this.fetch(`${this.endpoint}/ListAccountAliases`, {
      method: 'POST',
      headers,
      body: JSON.stringify(toJson(ListAccountAliasesRequestSchema, requestMessage)),
    });

    if (!response.ok) {
      const error = await response.text();
      throw new Error(`ListAccountAliases failed: ${response.status} ${error}`);
    }

    const responseData = (await response.json()) as any;
    return fromJson(ListAccountAliasesResponseSchema, responseData);
  }

  /**
   * CreateAlias creates a new alias for an account (validates domain is configured).
   */
  async createAlias(request: CreateAliasRequestData): Promise<CreateAliasResponse> {
    const headers: Record<string, string> = {
      'Content-Type': 'application/json',
    };

    if (this.authorization) {
      headers['Authorization'] = this.authorization;
    }

    const requestMessage = create(CreateAliasRequestSchema, request || {});
    const response = await this.fetch(`${this.endpoint}/CreateAlias`, {
      method: 'POST',
      headers,
      body: JSON.stringify(toJson(CreateAliasRequestSchema, requestMessage)),
    });

    if (!response.ok) {
      const error = await response.text();
      throw new Error(`CreateAlias failed: ${response.status} ${error}`);
    }

    const responseData = (await response.json()) as any;
    return fromJson(CreateAliasResponseSchema, responseData);
  }

  /**
   * UpdateAlias updates alias properties (IsActive, IsVisible).
   */
  async updateAlias(request: UpdateAliasRequestData): Promise<UpdateAliasResponse> {
    const headers: Record<string, string> = {
      'Content-Type': 'application/json',
    };

    if (this.authorization) {
      headers['Authorization'] = this.authorization;
    }

    const requestMessage = create(UpdateAliasRequestSchema, request || {});
    const response = await this.fetch(`${this.endpoint}/UpdateAlias`, {
      method: 'POST',
      headers,
      body: JSON.stringify(toJson(UpdateAliasRequestSchema, requestMessage)),
    });

    if (!response.ok) {
      const error = await response.text();
      throw new Error(`UpdateAlias failed: ${response.status} ${error}`);
    }

    const responseData = (await response.json()) as any;
    return fromJson(UpdateAliasResponseSchema, responseData);
  }

  /**
   * DeleteAlias deletes an alias (can delete primary aliases).
   */
  async deleteAlias(request: DeleteAliasRequestData): Promise<DeleteAliasResponse> {
    const headers: Record<string, string> = {
      'Content-Type': 'application/json',
    };

    if (this.authorization) {
      headers['Authorization'] = this.authorization;
    }

    const requestMessage = create(DeleteAliasRequestSchema, request || {});
    const response = await this.fetch(`${this.endpoint}/DeleteAlias`, {
      method: 'POST',
      headers,
      body: JSON.stringify(toJson(DeleteAliasRequestSchema, requestMessage)),
    });

    if (!response.ok) {
      const error = await response.text();
      throw new Error(`DeleteAlias failed: ${response.status} ${error}`);
    }

    const responseData = (await response.json()) as any;
    return fromJson(DeleteAliasResponseSchema, responseData);
  }

}

// Re-export types from protobuf
export * from './aliases_pb';
