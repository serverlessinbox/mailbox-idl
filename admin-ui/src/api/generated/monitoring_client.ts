// Code generated by protoc-gen-ts-client. DO NOT EDIT.

import type { Message } from '@bufbuild/protobuf';
import { create, toJson, fromJson } from '@bufbuild/protobuf';

import type {
  GetOverviewRequest,
  GetOverviewResponse,
  ListRecentErrorsRequest,
  ListRecentErrorsResponse,
  GetLogContextRequest,
  GetLogContextResponse,
} from './monitoring_pb';

import {
  GetOverviewRequestSchema,
  GetOverviewResponseSchema,
  ListRecentErrorsRequestSchema,
  ListRecentErrorsResponseSchema,
  GetLogContextRequestSchema,
  GetLogContextResponseSchema,
} from './monitoring_pb';

// Request types without protobuf Message metadata
type GetOverviewRequestData = Omit<GetOverviewRequest, keyof Message>;
type ListRecentErrorsRequestData = Omit<ListRecentErrorsRequest, keyof Message>;
type GetLogContextRequestData = Omit<GetLogContextRequest, keyof Message>;

export interface MonitoringServiceClientConfig {
  endpoint: string;
  authorization?: string;
  /** Custom fetch function for request interceptors, token refresh, retry logic, etc. */
  fetch?: typeof fetch;
}

/**
 * MonitoringService provides low-cost, on-demand observability endpoints for the Admin UI.
 * 
 * Design goals:
 * - Prefer CloudWatch metrics + Logs Insights queries (no always-on alarms/dashboards).
 * - Support a lightweight "drill-down" flow from error summaries to surrounding log context.
 */
export class MonitoringServiceClient {
  private readonly endpoint: string;
  private readonly authorization?: string;
  private readonly fetch: typeof fetch;

  constructor(config: MonitoringServiceClientConfig) {
    this.endpoint = config.endpoint.replace(/\/$/, '');
    this.authorization = config.authorization;
    this.fetch = config.fetch || globalThis.fetch.bind(globalThis);
  }

  /**
   * GetOverview returns a coarse, low-cost snapshot of key metrics.
   */
  async getOverview(request: GetOverviewRequestData): Promise<GetOverviewResponse> {
    const headers: Record<string, string> = {
      'Content-Type': 'application/json',
    };

    if (this.authorization) {
      headers['Authorization'] = this.authorization;
    }

    const requestMessage = create(GetOverviewRequestSchema, request || {});
    const response = await this.fetch(`${this.endpoint}/GetOverview`, {
      method: 'POST',
      headers,
      body: JSON.stringify(toJson(GetOverviewRequestSchema, requestMessage)),
    });

    if (!response.ok) {
      const error = await response.text();
      throw new Error(`GetOverview failed: ${response.status} ${error}`);
    }

    const responseData = (await response.json()) as any;
    return fromJson(GetOverviewResponseSchema, responseData);
  }

  /**
   * ListRecentErrors returns recent error log entries across configured log groups.
   */
  async listRecentErrors(request: ListRecentErrorsRequestData): Promise<ListRecentErrorsResponse> {
    const headers: Record<string, string> = {
      'Content-Type': 'application/json',
    };

    if (this.authorization) {
      headers['Authorization'] = this.authorization;
    }

    const requestMessage = create(ListRecentErrorsRequestSchema, request || {});
    const response = await this.fetch(`${this.endpoint}/ListRecentErrors`, {
      method: 'POST',
      headers,
      body: JSON.stringify(toJson(ListRecentErrorsRequestSchema, requestMessage)),
    });

    if (!response.ok) {
      const error = await response.text();
      throw new Error(`ListRecentErrors failed: ${response.status} ${error}`);
    }

    const responseData = (await response.json()) as any;
    return fromJson(ListRecentErrorsResponseSchema, responseData);
  }

  /**
   * GetLogContext returns log lines around a specific timestamp for drill-down.
   */
  async getLogContext(request: GetLogContextRequestData): Promise<GetLogContextResponse> {
    const headers: Record<string, string> = {
      'Content-Type': 'application/json',
    };

    if (this.authorization) {
      headers['Authorization'] = this.authorization;
    }

    const requestMessage = create(GetLogContextRequestSchema, request || {});
    const response = await this.fetch(`${this.endpoint}/GetLogContext`, {
      method: 'POST',
      headers,
      body: JSON.stringify(toJson(GetLogContextRequestSchema, requestMessage)),
    });

    if (!response.ok) {
      const error = await response.text();
      throw new Error(`GetLogContext failed: ${response.status} ${error}`);
    }

    const responseData = (await response.json()) as any;
    return fromJson(GetLogContextResponseSchema, responseData);
  }

}

// Re-export types from protobuf
export * from './monitoring_pb';
