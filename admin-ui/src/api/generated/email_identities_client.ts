// Code generated by protoc-gen-ts-client. DO NOT EDIT.

import type { Message } from '@bufbuild/protobuf';
import { create, toJson, fromJson } from '@bufbuild/protobuf';

import type {
  AddEmailIdentityRequest,
  AddEmailIdentityResponse,
  ListEmailIdentitiesRequest,
  ListEmailIdentitiesResponse,
  RemoveEmailIdentityRequest,
  RemoveEmailIdentityResponse,
  ResendVerificationEmailRequest,
  ResendVerificationEmailResponse,
} from './email_identities_pb';

import {
  AddEmailIdentityRequestSchema,
  AddEmailIdentityResponseSchema,
  ListEmailIdentitiesRequestSchema,
  ListEmailIdentitiesResponseSchema,
  RemoveEmailIdentityRequestSchema,
  RemoveEmailIdentityResponseSchema,
  ResendVerificationEmailRequestSchema,
  ResendVerificationEmailResponseSchema,
} from './email_identities_pb';

// Request types without protobuf Message metadata
type AddEmailIdentityRequestData = Omit<AddEmailIdentityRequest, keyof Message>;
type ListEmailIdentitiesRequestData = Omit<ListEmailIdentitiesRequest, keyof Message>;
type RemoveEmailIdentityRequestData = Omit<RemoveEmailIdentityRequest, keyof Message>;
type ResendVerificationEmailRequestData = Omit<ResendVerificationEmailRequest, keyof Message>;

export interface EmailIdentitiesServiceClientConfig {
  endpoint: string;
  authorization?: string;
  /** Custom fetch function for request interceptors, token refresh, retry logic, etc. */
  fetch?: typeof fetch;
}

/**
 * EmailIdentitiesService manages email identities for AWS SES.
 * Use this service to register, verify, and manage email addresses
 * that can be used as email senders.
 * 
 * Typical Workflow:
 * 1. Call AddEmailIdentity() to register an email
 * 2. Recipient receives verification email
 * 3. Recipient clicks verification link
 * 4. Call ListEmailIdentities() to check verification_status
 * 5. Once VERIFICATION_STATUS_SUCCESS, emails can be sent from this address
 */
export class EmailIdentitiesServiceClient {
  private readonly endpoint: string;
  private readonly authorization?: string;
  private readonly fetch: typeof fetch;

  constructor(config: EmailIdentitiesServiceClientConfig) {
    this.endpoint = config.endpoint.replace(/\/$/, '');
    this.authorization = config.authorization;
    this.fetch = config.fetch || globalThis.fetch.bind(globalThis);
  }

  /**
   * AddEmailIdentity registers a new email address with SES.
   */
  async addEmailIdentity(request: AddEmailIdentityRequestData): Promise<AddEmailIdentityResponse> {
    const headers: Record<string, string> = {
      'Content-Type': 'application/json',
    };

    if (this.authorization) {
      headers['Authorization'] = this.authorization;
    }

    const requestMessage = create(AddEmailIdentityRequestSchema, request || {});
    const response = await this.fetch(`${this.endpoint}/AddEmailIdentity`, {
      method: 'POST',
      headers,
      body: JSON.stringify(toJson(AddEmailIdentityRequestSchema, requestMessage)),
    });

    if (!response.ok) {
      const error = await response.text();
      throw new Error(`AddEmailIdentity failed: ${response.status} ${error}`);
    }

    const responseData = (await response.json()) as any;
    return fromJson(AddEmailIdentityResponseSchema, responseData);
  }

  /**
   * ListEmailIdentities retrieves all registered email identities.
   */
  async listEmailIdentities(request?: ListEmailIdentitiesRequestData): Promise<ListEmailIdentitiesResponse> {
    const headers: Record<string, string> = {
      'Content-Type': 'application/json',
    };

    if (this.authorization) {
      headers['Authorization'] = this.authorization;
    }

    const requestMessage = create(ListEmailIdentitiesRequestSchema, request || {});
    const response = await this.fetch(`${this.endpoint}/ListEmailIdentities`, {
      method: 'POST',
      headers,
      body: JSON.stringify(toJson(ListEmailIdentitiesRequestSchema, requestMessage)),
    });

    if (!response.ok) {
      const error = await response.text();
      throw new Error(`ListEmailIdentities failed: ${response.status} ${error}`);
    }

    const responseData = (await response.json()) as any;
    return fromJson(ListEmailIdentitiesResponseSchema, responseData);
  }

  /**
   * RemoveEmailIdentity deletes an email identity (cannot be undone).
   */
  async removeEmailIdentity(request: RemoveEmailIdentityRequestData): Promise<RemoveEmailIdentityResponse> {
    const headers: Record<string, string> = {
      'Content-Type': 'application/json',
    };

    if (this.authorization) {
      headers['Authorization'] = this.authorization;
    }

    const requestMessage = create(RemoveEmailIdentityRequestSchema, request || {});
    const response = await this.fetch(`${this.endpoint}/RemoveEmailIdentity`, {
      method: 'POST',
      headers,
      body: JSON.stringify(toJson(RemoveEmailIdentityRequestSchema, requestMessage)),
    });

    if (!response.ok) {
      const error = await response.text();
      throw new Error(`RemoveEmailIdentity failed: ${response.status} ${error}`);
    }

    const responseData = (await response.json()) as any;
    return fromJson(RemoveEmailIdentityResponseSchema, responseData);
  }

  /**
   * ResendVerificationEmail sends another verification email for an identity.
   */
  async resendVerificationEmail(request: ResendVerificationEmailRequestData): Promise<ResendVerificationEmailResponse> {
    const headers: Record<string, string> = {
      'Content-Type': 'application/json',
    };

    if (this.authorization) {
      headers['Authorization'] = this.authorization;
    }

    const requestMessage = create(ResendVerificationEmailRequestSchema, request || {});
    const response = await this.fetch(`${this.endpoint}/ResendVerificationEmail`, {
      method: 'POST',
      headers,
      body: JSON.stringify(toJson(ResendVerificationEmailRequestSchema, requestMessage)),
    });

    if (!response.ok) {
      const error = await response.text();
      throw new Error(`ResendVerificationEmail failed: ${response.status} ${error}`);
    }

    const responseData = (await response.json()) as any;
    return fromJson(ResendVerificationEmailResponseSchema, responseData);
  }

}

// Re-export types from protobuf
export * from './email_identities_pb';
