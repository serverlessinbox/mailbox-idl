// Code generated by protoc-gen-ts-client. DO NOT EDIT.

import type { Message } from '@bufbuild/protobuf';
import { create, toJson, fromJson } from '@bufbuild/protobuf';

import type {
  CreateGroupPrincipalRequest,
  CreateGroupPrincipalResponse,
  GetGroupPrincipalRequest,
  GetGroupPrincipalResponse,
  ListGroupPrincipalsRequest,
  ListGroupPrincipalsResponse,
  UpdateGroupPrincipalRequest,
  UpdateGroupPrincipalResponse,
  DeleteGroupPrincipalRequest,
  DeleteGroupPrincipalResponse,
  TransferGroupAccountOwnershipRequest,
  TransferGroupAccountOwnershipResponse,
  AddGroupAccountCapabilityRequest,
  AddGroupAccountCapabilityResponse,
  AddGroupMemberRequest,
  AddGroupMemberResponse,
  RemoveGroupMemberRequest,
  RemoveGroupMemberResponse,
  ListGroupMembersRequest,
  ListGroupMembersResponse,
} from './groups_pb';

import {
  CreateGroupPrincipalRequestSchema,
  CreateGroupPrincipalResponseSchema,
  GetGroupPrincipalRequestSchema,
  GetGroupPrincipalResponseSchema,
  ListGroupPrincipalsRequestSchema,
  ListGroupPrincipalsResponseSchema,
  UpdateGroupPrincipalRequestSchema,
  UpdateGroupPrincipalResponseSchema,
  DeleteGroupPrincipalRequestSchema,
  DeleteGroupPrincipalResponseSchema,
  TransferGroupAccountOwnershipRequestSchema,
  TransferGroupAccountOwnershipResponseSchema,
  AddGroupAccountCapabilityRequestSchema,
  AddGroupAccountCapabilityResponseSchema,
  AddGroupMemberRequestSchema,
  AddGroupMemberResponseSchema,
  RemoveGroupMemberRequestSchema,
  RemoveGroupMemberResponseSchema,
  ListGroupMembersRequestSchema,
  ListGroupMembersResponseSchema,
} from './groups_pb';

// Request types without protobuf Message metadata
type CreateGroupPrincipalRequestData = Omit<CreateGroupPrincipalRequest, keyof Message>;
type GetGroupPrincipalRequestData = Omit<GetGroupPrincipalRequest, keyof Message>;
type ListGroupPrincipalsRequestData = Omit<ListGroupPrincipalsRequest, keyof Message>;
type UpdateGroupPrincipalRequestData = Omit<UpdateGroupPrincipalRequest, keyof Message>;
type DeleteGroupPrincipalRequestData = Omit<DeleteGroupPrincipalRequest, keyof Message>;
type TransferGroupAccountOwnershipRequestData = Omit<TransferGroupAccountOwnershipRequest, keyof Message>;
type AddGroupAccountCapabilityRequestData = Omit<AddGroupAccountCapabilityRequest, keyof Message>;
type AddGroupMemberRequestData = Omit<AddGroupMemberRequest, keyof Message>;
type RemoveGroupMemberRequestData = Omit<RemoveGroupMemberRequest, keyof Message>;
type ListGroupMembersRequestData = Omit<ListGroupMembersRequest, keyof Message>;

export interface GroupsServiceClientConfig {
  endpoint: string;
  authorization?: string;
  /** Custom fetch function for request interceptors, token refresh, retry logic, etc. */
  fetch?: typeof fetch;
}

/**
 * GroupsService manages group principals and their associated accounts.
 * A group principal owns a shared account that can be accessed by multiple users via JMAP ObjectGrants.
 */
export class GroupsServiceClient {
  private readonly endpoint: string;
  private readonly authorization?: string;
  private readonly fetch: typeof fetch;

  constructor(config: GroupsServiceClientConfig) {
    this.endpoint = config.endpoint.replace(/\/$/, '');
    this.authorization = config.authorization;
    this.fetch = config.fetch || globalThis.fetch.bind(globalThis);
  }

  /**
   * CreateGroupPrincipal provisions a new group principal + its account with the given capabilities.
   */
  async createGroupPrincipal(request: CreateGroupPrincipalRequestData): Promise<CreateGroupPrincipalResponse> {
    const headers: Record<string, string> = {
      'Content-Type': 'application/json',
    };

    if (this.authorization) {
      headers['Authorization'] = this.authorization;
    }

    const requestMessage = create(CreateGroupPrincipalRequestSchema, request || {});
    const response = await this.fetch(`${this.endpoint}/CreateGroupPrincipal`, {
      method: 'POST',
      headers,
      body: JSON.stringify(toJson(CreateGroupPrincipalRequestSchema, requestMessage)),
    });

    if (!response.ok) {
      const error = await response.text();
      throw new Error(`CreateGroupPrincipal failed: ${response.status} ${error}`);
    }

    const responseData = (await response.json()) as any;
    return fromJson(CreateGroupPrincipalResponseSchema, responseData);
  }

  /**
   * GetGroupPrincipal retrieves a group principal and its account.
   */
  async getGroupPrincipal(request: GetGroupPrincipalRequestData): Promise<GetGroupPrincipalResponse> {
    const headers: Record<string, string> = {
      'Content-Type': 'application/json',
    };

    if (this.authorization) {
      headers['Authorization'] = this.authorization;
    }

    const requestMessage = create(GetGroupPrincipalRequestSchema, request || {});
    const response = await this.fetch(`${this.endpoint}/GetGroupPrincipal`, {
      method: 'POST',
      headers,
      body: JSON.stringify(toJson(GetGroupPrincipalRequestSchema, requestMessage)),
    });

    if (!response.ok) {
      const error = await response.text();
      throw new Error(`GetGroupPrincipal failed: ${response.status} ${error}`);
    }

    const responseData = (await response.json()) as any;
    return fromJson(GetGroupPrincipalResponseSchema, responseData);
  }

  /**
   * ListGroupPrincipals lists all group principals for the tenant.
   */
  async listGroupPrincipals(request?: ListGroupPrincipalsRequestData): Promise<ListGroupPrincipalsResponse> {
    const headers: Record<string, string> = {
      'Content-Type': 'application/json',
    };

    if (this.authorization) {
      headers['Authorization'] = this.authorization;
    }

    const requestMessage = create(ListGroupPrincipalsRequestSchema, request || {});
    const response = await this.fetch(`${this.endpoint}/ListGroupPrincipals`, {
      method: 'POST',
      headers,
      body: JSON.stringify(toJson(ListGroupPrincipalsRequestSchema, requestMessage)),
    });

    if (!response.ok) {
      const error = await response.text();
      throw new Error(`ListGroupPrincipals failed: ${response.status} ${error}`);
    }

    const responseData = (await response.json()) as any;
    return fromJson(ListGroupPrincipalsResponseSchema, responseData);
  }

  /**
   * UpdateGroupPrincipal renames a group principal.
   */
  async updateGroupPrincipal(request: UpdateGroupPrincipalRequestData): Promise<UpdateGroupPrincipalResponse> {
    const headers: Record<string, string> = {
      'Content-Type': 'application/json',
    };

    if (this.authorization) {
      headers['Authorization'] = this.authorization;
    }

    const requestMessage = create(UpdateGroupPrincipalRequestSchema, request || {});
    const response = await this.fetch(`${this.endpoint}/UpdateGroupPrincipal`, {
      method: 'POST',
      headers,
      body: JSON.stringify(toJson(UpdateGroupPrincipalRequestSchema, requestMessage)),
    });

    if (!response.ok) {
      const error = await response.text();
      throw new Error(`UpdateGroupPrincipal failed: ${response.status} ${error}`);
    }

    const responseData = (await response.json()) as any;
    return fromJson(UpdateGroupPrincipalResponseSchema, responseData);
  }

  /**
   * DeleteGroupPrincipal removes the group principal and its account (cascade delete).
   */
  async deleteGroupPrincipal(request: DeleteGroupPrincipalRequestData): Promise<DeleteGroupPrincipalResponse> {
    const headers: Record<string, string> = {
      'Content-Type': 'application/json',
    };

    if (this.authorization) {
      headers['Authorization'] = this.authorization;
    }

    const requestMessage = create(DeleteGroupPrincipalRequestSchema, request || {});
    const response = await this.fetch(`${this.endpoint}/DeleteGroupPrincipal`, {
      method: 'POST',
      headers,
      body: JSON.stringify(toJson(DeleteGroupPrincipalRequestSchema, requestMessage)),
    });

    if (!response.ok) {
      const error = await response.text();
      throw new Error(`DeleteGroupPrincipal failed: ${response.status} ${error}`);
    }

    const responseData = (await response.json()) as any;
    return fromJson(DeleteGroupPrincipalResponseSchema, responseData);
  }

  /**
   * TransferGroupAccountOwnership transfers administrative ownership of a group account to a new user principal.
   */
  async transferGroupAccountOwnership(request: TransferGroupAccountOwnershipRequestData): Promise<TransferGroupAccountOwnershipResponse> {
    const headers: Record<string, string> = {
      'Content-Type': 'application/json',
    };

    if (this.authorization) {
      headers['Authorization'] = this.authorization;
    }

    const requestMessage = create(TransferGroupAccountOwnershipRequestSchema, request || {});
    const response = await this.fetch(`${this.endpoint}/TransferGroupAccountOwnership`, {
      method: 'POST',
      headers,
      body: JSON.stringify(toJson(TransferGroupAccountOwnershipRequestSchema, requestMessage)),
    });

    if (!response.ok) {
      const error = await response.text();
      throw new Error(`TransferGroupAccountOwnership failed: ${response.status} ${error}`);
    }

    const responseData = (await response.json()) as any;
    return fromJson(TransferGroupAccountOwnershipResponseSchema, responseData);
  }

  /**
   * AddGroupAccountCapability idempotently adds a new JMAP capability to a group account and provisions the required resources.
   */
  async addGroupAccountCapability(request: AddGroupAccountCapabilityRequestData): Promise<AddGroupAccountCapabilityResponse> {
    const headers: Record<string, string> = {
      'Content-Type': 'application/json',
    };

    if (this.authorization) {
      headers['Authorization'] = this.authorization;
    }

    const requestMessage = create(AddGroupAccountCapabilityRequestSchema, request || {});
    const response = await this.fetch(`${this.endpoint}/AddGroupAccountCapability`, {
      method: 'POST',
      headers,
      body: JSON.stringify(toJson(AddGroupAccountCapabilityRequestSchema, requestMessage)),
    });

    if (!response.ok) {
      const error = await response.text();
      throw new Error(`AddGroupAccountCapability failed: ${response.status} ${error}`);
    }

    const responseData = (await response.json()) as any;
    return fromJson(AddGroupAccountCapabilityResponseSchema, responseData);
  }

  /**
   * AddGroupMember adds a user principal as a member of a group.
   */
  async addGroupMember(request: AddGroupMemberRequestData): Promise<AddGroupMemberResponse> {
    const headers: Record<string, string> = {
      'Content-Type': 'application/json',
    };

    if (this.authorization) {
      headers['Authorization'] = this.authorization;
    }

    const requestMessage = create(AddGroupMemberRequestSchema, request || {});
    const response = await this.fetch(`${this.endpoint}/AddGroupMember`, {
      method: 'POST',
      headers,
      body: JSON.stringify(toJson(AddGroupMemberRequestSchema, requestMessage)),
    });

    if (!response.ok) {
      const error = await response.text();
      throw new Error(`AddGroupMember failed: ${response.status} ${error}`);
    }

    const responseData = (await response.json()) as any;
    return fromJson(AddGroupMemberResponseSchema, responseData);
  }

  /**
   * RemoveGroupMember removes a user principal from a group.
   */
  async removeGroupMember(request: RemoveGroupMemberRequestData): Promise<RemoveGroupMemberResponse> {
    const headers: Record<string, string> = {
      'Content-Type': 'application/json',
    };

    if (this.authorization) {
      headers['Authorization'] = this.authorization;
    }

    const requestMessage = create(RemoveGroupMemberRequestSchema, request || {});
    const response = await this.fetch(`${this.endpoint}/RemoveGroupMember`, {
      method: 'POST',
      headers,
      body: JSON.stringify(toJson(RemoveGroupMemberRequestSchema, requestMessage)),
    });

    if (!response.ok) {
      const error = await response.text();
      throw new Error(`RemoveGroupMember failed: ${response.status} ${error}`);
    }

    const responseData = (await response.json()) as any;
    return fromJson(RemoveGroupMemberResponseSchema, responseData);
  }

  /**
   * ListGroupMembers lists all user principals that are members of a group.
   */
  async listGroupMembers(request: ListGroupMembersRequestData): Promise<ListGroupMembersResponse> {
    const headers: Record<string, string> = {
      'Content-Type': 'application/json',
    };

    if (this.authorization) {
      headers['Authorization'] = this.authorization;
    }

    const requestMessage = create(ListGroupMembersRequestSchema, request || {});
    const response = await this.fetch(`${this.endpoint}/ListGroupMembers`, {
      method: 'POST',
      headers,
      body: JSON.stringify(toJson(ListGroupMembersRequestSchema, requestMessage)),
    });

    if (!response.ok) {
      const error = await response.text();
      throw new Error(`ListGroupMembers failed: ${response.status} ${error}`);
    }

    const responseData = (await response.json()) as any;
    return fromJson(ListGroupMembersResponseSchema, responseData);
  }

}

// Re-export types from protobuf
export * from './groups_pb';
