// Code generated by protoc-gen-ts-client. DO NOT EDIT.

import type { Message } from '@bufbuild/protobuf';
import { create, toJson, fromJson } from '@bufbuild/protobuf';

import type {
  CreateSharedMailboxRequest,
  CreateSharedMailboxResponse,
  GetSharedMailboxRequest,
  GetSharedMailboxResponse,
  ListSharedMailboxesRequest,
  ListSharedMailboxesResponse,
  UpdateSharedMailboxRequest,
  UpdateSharedMailboxResponse,
  DeleteSharedMailboxRequest,
  DeleteSharedMailboxResponse,
  AddDelegateRequest,
  AddDelegateResponse,
  RemoveDelegateRequest,
  RemoveDelegateResponse,
  ListDelegatesRequest,
  ListDelegatesResponse,
  AddSharedMailboxAccountCapabilityRequest,
  AddSharedMailboxAccountCapabilityResponse,
} from './shared_mailboxes_pb';

import {
  CreateSharedMailboxRequestSchema,
  CreateSharedMailboxResponseSchema,
  GetSharedMailboxRequestSchema,
  GetSharedMailboxResponseSchema,
  ListSharedMailboxesRequestSchema,
  ListSharedMailboxesResponseSchema,
  UpdateSharedMailboxRequestSchema,
  UpdateSharedMailboxResponseSchema,
  DeleteSharedMailboxRequestSchema,
  DeleteSharedMailboxResponseSchema,
  AddDelegateRequestSchema,
  AddDelegateResponseSchema,
  RemoveDelegateRequestSchema,
  RemoveDelegateResponseSchema,
  ListDelegatesRequestSchema,
  ListDelegatesResponseSchema,
  AddSharedMailboxAccountCapabilityRequestSchema,
  AddSharedMailboxAccountCapabilityResponseSchema,
} from './shared_mailboxes_pb';

// Request types without protobuf Message metadata
type CreateSharedMailboxRequestData = Omit<CreateSharedMailboxRequest, keyof Message>;
type GetSharedMailboxRequestData = Omit<GetSharedMailboxRequest, keyof Message>;
type ListSharedMailboxesRequestData = Omit<ListSharedMailboxesRequest, keyof Message>;
type UpdateSharedMailboxRequestData = Omit<UpdateSharedMailboxRequest, keyof Message>;
type DeleteSharedMailboxRequestData = Omit<DeleteSharedMailboxRequest, keyof Message>;
type AddDelegateRequestData = Omit<AddDelegateRequest, keyof Message>;
type RemoveDelegateRequestData = Omit<RemoveDelegateRequest, keyof Message>;
type ListDelegatesRequestData = Omit<ListDelegatesRequest, keyof Message>;
type AddSharedMailboxAccountCapabilityRequestData = Omit<AddSharedMailboxAccountCapabilityRequest, keyof Message>;

export interface SharedMailboxesServiceClientConfig {
  endpoint: string;
  authorization?: string;
  /** Custom fetch function for request interceptors, token refresh, retry logic, etc. */
  fetch?: typeof fetch;
}

/**
 * SharedMailboxesService manages shared mailbox principals and their delegates.
 * A shared mailbox is a resource mailbox (mailbox, room, or equipment) that can be
 * accessed by multiple users via delegation.
 */
export class SharedMailboxesServiceClient {
  private readonly endpoint: string;
  private readonly authorization?: string;
  private readonly fetch: typeof fetch;

  constructor(config: SharedMailboxesServiceClientConfig) {
    this.endpoint = config.endpoint.replace(/\/$/, '');
    this.authorization = config.authorization;
    this.fetch = config.fetch || globalThis.fetch.bind(globalThis);
  }

  /**
   * CreateSharedMailbox provisions a new shared mailbox with the given identity.
   */
  async createSharedMailbox(request: CreateSharedMailboxRequestData): Promise<CreateSharedMailboxResponse> {
    const headers: Record<string, string> = {
      'Content-Type': 'application/json',
    };

    if (this.authorization) {
      headers['Authorization'] = this.authorization;
    }

    const requestMessage = create(CreateSharedMailboxRequestSchema, request || {});
    const response = await this.fetch(`${this.endpoint}/CreateSharedMailbox`, {
      method: 'POST',
      headers,
      body: JSON.stringify(toJson(CreateSharedMailboxRequestSchema, requestMessage)),
    });

    if (!response.ok) {
      const error = await response.text();
      throw new Error(`CreateSharedMailbox failed: ${response.status} ${error}`);
    }

    const responseData = (await response.json()) as any;
    return fromJson(CreateSharedMailboxResponseSchema, responseData);
  }

  /**
   * GetSharedMailbox retrieves a shared mailbox by ID.
   */
  async getSharedMailbox(request: GetSharedMailboxRequestData): Promise<GetSharedMailboxResponse> {
    const headers: Record<string, string> = {
      'Content-Type': 'application/json',
    };

    if (this.authorization) {
      headers['Authorization'] = this.authorization;
    }

    const requestMessage = create(GetSharedMailboxRequestSchema, request || {});
    const response = await this.fetch(`${this.endpoint}/GetSharedMailbox`, {
      method: 'POST',
      headers,
      body: JSON.stringify(toJson(GetSharedMailboxRequestSchema, requestMessage)),
    });

    if (!response.ok) {
      const error = await response.text();
      throw new Error(`GetSharedMailbox failed: ${response.status} ${error}`);
    }

    const responseData = (await response.json()) as any;
    return fromJson(GetSharedMailboxResponseSchema, responseData);
  }

  /**
   * ListSharedMailboxes lists all shared mailboxes for the tenant.
   */
  async listSharedMailboxes(request?: ListSharedMailboxesRequestData): Promise<ListSharedMailboxesResponse> {
    const headers: Record<string, string> = {
      'Content-Type': 'application/json',
    };

    if (this.authorization) {
      headers['Authorization'] = this.authorization;
    }

    const requestMessage = create(ListSharedMailboxesRequestSchema, request || {});
    const response = await this.fetch(`${this.endpoint}/ListSharedMailboxes`, {
      method: 'POST',
      headers,
      body: JSON.stringify(toJson(ListSharedMailboxesRequestSchema, requestMessage)),
    });

    if (!response.ok) {
      const error = await response.text();
      throw new Error(`ListSharedMailboxes failed: ${response.status} ${error}`);
    }

    const responseData = (await response.json()) as any;
    return fromJson(ListSharedMailboxesResponseSchema, responseData);
  }

  /**
   * UpdateSharedMailbox updates shared mailbox properties.
   */
  async updateSharedMailbox(request: UpdateSharedMailboxRequestData): Promise<UpdateSharedMailboxResponse> {
    const headers: Record<string, string> = {
      'Content-Type': 'application/json',
    };

    if (this.authorization) {
      headers['Authorization'] = this.authorization;
    }

    const requestMessage = create(UpdateSharedMailboxRequestSchema, request || {});
    const response = await this.fetch(`${this.endpoint}/UpdateSharedMailbox`, {
      method: 'POST',
      headers,
      body: JSON.stringify(toJson(UpdateSharedMailboxRequestSchema, requestMessage)),
    });

    if (!response.ok) {
      const error = await response.text();
      throw new Error(`UpdateSharedMailbox failed: ${response.status} ${error}`);
    }

    const responseData = (await response.json()) as any;
    return fromJson(UpdateSharedMailboxResponseSchema, responseData);
  }

  /**
   * DeleteSharedMailbox removes the shared mailbox and its account.
   */
  async deleteSharedMailbox(request: DeleteSharedMailboxRequestData): Promise<DeleteSharedMailboxResponse> {
    const headers: Record<string, string> = {
      'Content-Type': 'application/json',
    };

    if (this.authorization) {
      headers['Authorization'] = this.authorization;
    }

    const requestMessage = create(DeleteSharedMailboxRequestSchema, request || {});
    const response = await this.fetch(`${this.endpoint}/DeleteSharedMailbox`, {
      method: 'POST',
      headers,
      body: JSON.stringify(toJson(DeleteSharedMailboxRequestSchema, requestMessage)),
    });

    if (!response.ok) {
      const error = await response.text();
      throw new Error(`DeleteSharedMailbox failed: ${response.status} ${error}`);
    }

    const responseData = (await response.json()) as any;
    return fromJson(DeleteSharedMailboxResponseSchema, responseData);
  }

  /**
   * AddDelegate grants a principal delegate access to a shared mailbox account.
   */
  async addDelegate(request: AddDelegateRequestData): Promise<AddDelegateResponse> {
    const headers: Record<string, string> = {
      'Content-Type': 'application/json',
    };

    if (this.authorization) {
      headers['Authorization'] = this.authorization;
    }

    const requestMessage = create(AddDelegateRequestSchema, request || {});
    const response = await this.fetch(`${this.endpoint}/AddDelegate`, {
      method: 'POST',
      headers,
      body: JSON.stringify(toJson(AddDelegateRequestSchema, requestMessage)),
    });

    if (!response.ok) {
      const error = await response.text();
      throw new Error(`AddDelegate failed: ${response.status} ${error}`);
    }

    const responseData = (await response.json()) as any;
    return fromJson(AddDelegateResponseSchema, responseData);
  }

  /**
   * RemoveDelegate revokes delegate access from a principal.
   */
  async removeDelegate(request: RemoveDelegateRequestData): Promise<RemoveDelegateResponse> {
    const headers: Record<string, string> = {
      'Content-Type': 'application/json',
    };

    if (this.authorization) {
      headers['Authorization'] = this.authorization;
    }

    const requestMessage = create(RemoveDelegateRequestSchema, request || {});
    const response = await this.fetch(`${this.endpoint}/RemoveDelegate`, {
      method: 'POST',
      headers,
      body: JSON.stringify(toJson(RemoveDelegateRequestSchema, requestMessage)),
    });

    if (!response.ok) {
      const error = await response.text();
      throw new Error(`RemoveDelegate failed: ${response.status} ${error}`);
    }

    const responseData = (await response.json()) as any;
    return fromJson(RemoveDelegateResponseSchema, responseData);
  }

  /**
   * ListDelegates lists all delegates for a shared mailbox account.
   */
  async listDelegates(request: ListDelegatesRequestData): Promise<ListDelegatesResponse> {
    const headers: Record<string, string> = {
      'Content-Type': 'application/json',
    };

    if (this.authorization) {
      headers['Authorization'] = this.authorization;
    }

    const requestMessage = create(ListDelegatesRequestSchema, request || {});
    const response = await this.fetch(`${this.endpoint}/ListDelegates`, {
      method: 'POST',
      headers,
      body: JSON.stringify(toJson(ListDelegatesRequestSchema, requestMessage)),
    });

    if (!response.ok) {
      const error = await response.text();
      throw new Error(`ListDelegates failed: ${response.status} ${error}`);
    }

    const responseData = (await response.json()) as any;
    return fromJson(ListDelegatesResponseSchema, responseData);
  }

  /**
   * AddSharedMailboxAccountCapability idempotently adds a new JMAP capability to a shared mailbox account and provisions the required resources.
   */
  async addSharedMailboxAccountCapability(request: AddSharedMailboxAccountCapabilityRequestData): Promise<AddSharedMailboxAccountCapabilityResponse> {
    const headers: Record<string, string> = {
      'Content-Type': 'application/json',
    };

    if (this.authorization) {
      headers['Authorization'] = this.authorization;
    }

    const requestMessage = create(AddSharedMailboxAccountCapabilityRequestSchema, request || {});
    const response = await this.fetch(`${this.endpoint}/AddSharedMailboxAccountCapability`, {
      method: 'POST',
      headers,
      body: JSON.stringify(toJson(AddSharedMailboxAccountCapabilityRequestSchema, requestMessage)),
    });

    if (!response.ok) {
      const error = await response.text();
      throw new Error(`AddSharedMailboxAccountCapability failed: ${response.status} ${error}`);
    }

    const responseData = (await response.json()) as any;
    return fromJson(AddSharedMailboxAccountCapabilityResponseSchema, responseData);
  }

}

// Re-export types from protobuf
export * from './shared_mailboxes_pb';
